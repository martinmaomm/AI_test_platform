# API 第三批补充验收

日期：2026-09-15。代码基线：`68fd4d5`。本批对应 [系统回归清单](../plans/2026-09-14-api-regression-checklist.md) 的邮件、权限、异常/报告边界和共享 UI 检查。

**结论：本批上述核心检查通过，两个实际数据访问漏洞已修复并在重启后的服务验证。带报告链接的真实邮件仍待用户授权补测，不能将接收组测试邮件等同于 SCH-05 全部通过。**

## 实际平台验证

- SMTP 配置 1 的网页“测试连接”返回 200；API 项目 2 的接收组 1“发送测试邮件”返回 200。本批只发送一封主题为「【自动化测试平台】邮件接收组测试」的邮件；用户已在对话确认收到。SMTP 接受和实际收信均有证据，不包含授权码或完整邮箱地址。
- 使用用户新建的普通账号经真实浏览器表单登录，未注入管理员 token：工作区 31 返回 403，API 报告 90、UI 执行 16 截图接口返回 404，邮件配置返回 403；页面显示“报告不存在或无权访问”。退出后再次访问报告跳回登录页。
- UI 用例 3 的脚本先审阅，确认只新增唯一名称角色并操作自己的新数据、不分配权限，再通过真实网页执行一次。执行 16 通过，用时约 5.84 秒；日志有 5 条验证通过和“测试用例执行完毕”，鉴权截图返回 200。独立查询被测平台完整角色列表，运行前后 ID 集合一致，本轮数据无残留，保存的脚本未改动。
- 实际平台模型配置、邮件设置、新项目知识库 `/web-testing/knowledge-base`、用例及执行记录页面只读冒烟通过；报告展开原始日志后可读、截图可见、刷新正常。未发起新 AI 生成、知识库处理或付费模型调用。

仅使用本轮自有 UI 执行 16 检查图片安全，不枚举历史文件。所有本地证据位于被 Git 忽略的 `backend/logs/api-regression/20260915-batch3-lxupvC/`；认证和原始报告保存在受限 private 文件，不提交。

## 新发现与修复范围

1. **API 规范列表跨项目读取**：普通非成员请求项目 2 的规范列表得到 200 和数据，实际平台及隔离浏览器均复现。用户已批准修复该边界以及相关无权限接口的 403/404 返回，不更改生成或执行业务。
2. **执行截图静态地址绕过鉴权**：匿名访问自有执行 16 的公开 `/media/` 地址返回 200，而正式截图 API 返回 401。用户已批准封闭公开路径；保留原文件和正常鉴权接口，同时检查规范化路径、编码、大小写和符号链接绕过。生产反向代理另需拒绝直接静态访问，Django 修复不能替代代理配置。
3. **相关权限响应不一致**：原部分列表返回空 200，详情返回 400，未在这些响应中发现数据或任务派发。这属于错误响应契约问题，严重度低于前两项实际数据暴露，不能混为一谈。

修复后结果见下方交付段。实际平台的初始失败证据单独保留；隔离浏览器矩阵文件记录最终复测，首次失败情况保留在专项报告中。

## 隔离验证

所有 Django 自动化测试在 `django.setup()` 前切换临时 SQLite、内存缓存/队列，并拦截非回环网络；没有执行默认 NAS 数据库上的 `manage.py test`，没有重跑真实业务写操作。

- API 异常、取消、模型故障分类、迟到结果、任务快照与请求检查点：120 项针对性测试通过。401、429、500、503、504、overload、流式中断都有固定错误归因；流中断保留既有候选和验证证据，不把未运行内容标成通过。详见 [权限和异常专项](2026-09-15-api-access-failures.md)。
- 主审独立执行通知服务/投递、计划任务原生报告及调度可靠性 41 项，通过；包括 always/fail 通知条件、部分收件人拒收、空计划、派发异常、通知标记和部分报告。预期注入的失败日志不代表测试失败。不同专项可能包含交集，不将计数直接相加为独立覆盖总量。
- 主审补充套件终态矩阵 5 组：零用例和全跳过 → skipped/0%；停止与执行异常 → error；缺少子结果 → incomplete。均无运行中假状态、无全部通过假象。该矩阵是隔离构造数据，不是五次真实套件运行。
- 真实 Vue/Django/Chrome 隔离专项通过：脚本助手候选编辑/验证/失败截图、三类原生报告、项目/模型/知识库/邮件只读页面。详见 [共享 UI 冒烟](2026-09-15-shared-ui-smoke.md)。其中隔离脚本的知识库为旧路由；实际平台已另检查新路由，不将两者混淆。

## 证据索引

- `mail-test.json`、`smtp-accepted.png`：测试邮件发送与用户收信确认。
- `ui-live.json`、`ui-report-header.png`、`ui-completion-screenshot.png`：真实 UI 执行、清理、日志和截图。
- `live-shared-readonly.json`：实际共享页面只读检查。
- `restricted-live-access-before-fix.json`、`restricted-ui-report.png`：普通用户真实登录/退出、拒绝访问页面和修复前列表泄漏。
- `anonymous-report-check.json`：修复前截图公开地址绕过。
- `terminal-report-matrix.json`：五组终态汇总。
- `live-access-after-fix.json`：公开截图路径封闭、原鉴权截图未改变、管理员正向读取。
- `restricted-live-access-after-fix.json`、`restricted-ui-report-after-fix.png`：新账号真实登录/退出，12 个读取接口均拒绝；管理员报告日志、截图和刷新正常。

## 修复交付与重启后验证

- 隔离浏览器矩阵：37 条检查记录（35 个 HTTP 检查），35 个 HTTP 结果为 404×13、403×21、200×1；唯一 200 为有报告权限的 viewer。无失败、无任务派发、无原数据修改。
- 子代理权限小集 10 项与媒体小集 6 项通过；主审在代码冻结后独立复跑 `common.test_public_media`、`api_testing.test_acceptance_faults`、`api_testing.test_execution_visibility`、`api_testing.test_browser_source_handoff`，**19/19 通过**。包含普通成员、独立 owner、superuser 原有报告权限、跨项目执行删除、普通公开媒体及私有截图绕过边界。
- 后端和 Celery 在 active/reserved/scheduled 均为 0 后优雅停止并重启，没有强杀在途任务；新 PID 分别为 96871、96872，8000 监听和 worker ready 已确认。未改前端源码，无需重启 Vite；无数据库迁移。
- 重启后普通账号通过真实浏览器登录，工作区、规范列表/详情/端点、用例列表/详情、执行列表/详情、API/UI/计划报告、SMTP 配置共 12 个读取接口全部 403/404 且无数据。退出后再次访问报告跳回登录。
- 匿名访问本轮 execution 16 的原始媒体路径、点路径、编码及大小写变体共 5 项全部 404；管理员直读媒体也为 404。原鉴权截图接口仍 200，52115 字节及 SHA-256 与修复前一致，说明未删除或替换截图。
- 管理员规范、端点、用例、API 报告及计划报告接口仍 200；真实 UI 报告日志、截图和刷新再次验证通过，无页面 JS 错误。真实写入型越权尝试仅在临时 SQLite 执行，未拿实际业务记录测试删除。

本次未修改模型请求、生成状态机、断言或运行器；未新发起真实生成任务。提交排除本地凭据、日志、浏览器压缩包和用户已有的未跟踪文档。

## 尚待补全的边界

- 当前 `FRONTEND_BASE_URL` 为 `http://127.0.0.1:5173`。其他设备收到报告邮件会指向自身；已请求用户授权改为局域网地址并补一封报告链接验收邮件。本次收到的接收组测试邮件不含报告链接，不能据此标记 SCH-05 全通过。
- `ProjectMember.role` 与独立能力标志分离：仅设置 viewer 不等于自动关闭执行权限。隔离只读账号已显式设置权限；本批不修改角色产品逻辑。
- 共享页面检查不等于知识库向量化、真实模型质量或所有 UI 网站流程验收；故障注入也不等于真实服务商长时间稳定性测试。本批没有重新启动 Beat 或重复触发计划任务。
