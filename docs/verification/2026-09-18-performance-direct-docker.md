# 性能节点单条 Docker 命令验收（2026-09-18）

## 结论

网页安装向导已由长 Bash 引导脚本改为单条 `docker run`，实际复制结果为 800 字符，无换行、内嵌 PEM 或架构分支脚本。新命令自动登记节点并运行，正常重启复用身份。已发布并激活 0.2.1；协议仍为 2、Locust 仍为 2.43.3，固定压测脚本未改。已有 0.2.0 节点不会被升级或重置。

## 自动化检查

| 范围 | 结果 |
| --- | --- |
| 节点客户端 unittest | 52 项通过 |
| 后端性能模块（内存 SQLite，禁止外部网络） | 71 项通过，无模型迁移 |
| 发布器 unittest | 32 项通过 |
| 全层镜像审计器 unittest | 8 项通过 |
| 前端性能工作区测试 | 17 项通过 |
| 前端生产构建 | 通过；仍有既有的大分块提示 |
| 最终不可变镜像的真实 Docker 启动 | amd64、arm64 各 3 个场景通过 |

真实 Docker 场景使用临时 HTTPS 服务，不连接业务数据库或被测商城、不发压：

- 首次注册、心跳、重启后继续心跳；每个架构注册仅一次，累计至少四次心跳。
- 错误 CA 指纹：未发送注册凭证，零注册、零心跳。
- 明确拒绝注册（401）：重启不重复提交同一令牌，注册一次、零心跳。
- 验证非 root UID10001、身份文件 0600、只读根文件系统、无 Docker socket，以及令牌不出现在 Agent 日志。
- 验收按镜像 ID 固定执行，检查版本和架构；临时容器、卷按本轮归属标签清理。

## 发布与敏感信息检查

仓库：[martinmao9/performance-node](https://hub.docker.com/r/martinmao9/performance-node)，公开、说明标注“仅限测试使用”。发布前对最终镜像的所有层执行定向敏感内容审计，覆盖应用白名单、私钥/令牌、本机平台敏感配置值和敏感路径；不把后续层删除视为历史层已移除。

| 架构 | 固定标签 | 镜像 manifest SHA256 |
| --- | --- | --- |
| amd64 | `0.2.1-amd64-c77900b94818` | `6286fe115f79ed4338c70ba82fc87857e398e32851b6c356e43811ce591f535a` |
| arm64 | `0.2.1-arm64-ebd82148d134` | `9871439a64ccba47ea4a6ac93381b16a2f9bf904fe92a8c7b8e3195171645413` |

网页实际使用固定多架构索引：

```text
docker.io/martinmao9/performance-node@sha256:49e0de3b7dbcb3387104fda9a86a666f338b977c74ce271a48a0cd797446bde6
```

发布器验证两个成员的远端配置、摘要和架构，再使用空凭据配置匿名读取并分别拉取索引的 amd64/arm64 镜像，通过后才写入 `registry_index_ref`。Docker Hub 简介和完整说明更新后通过匿名 API 回读确认。

发行目录为 Git 忽略的 `backend/resource/performance-node-dockerhub-0.2.1/`。同版本内容冲突拒绝覆盖；发布探针使用唯一名称和归属标签，超时后核验归属再清理，不遗留后台容器或误报清理成功。

## 真实网页到平台的联调

使用本机 Chrome 打开正在运行的前端，在已有性能联调项目中创建专用临时节点；没有改动原有节点、目标或压测计划。

1. 网页创建节点返回 201，复制按钮内容与后端命令完全一致；浏览器无页面错误。
2. 直接执行复制出的命令，通过实际 `home.maoyijiu.top:18443` 控制入口登记，平台确认 0.2.1 节点在线。
3. 重启该容器后，身份文件摘要及注册时间不变，平台收到新的心跳。
4. 容器检查确认只读、非 root、非 privileged、没有挂载 Docker socket。
5. 验收后吊销并从列表移除临时节点，删除对应临时容器和身份卷；未启动任何压测，未删除原有节点。平台软删除审计记录保留。

联调首轮发现并修复 CA 端点的真实问题：Agent 使用 `Accept: application/x-pem-file`，原 DRF 内容协商返回 406。CA 端点改为只提供 GET/HEAD 的普通 Django 文件响应，补充同请求头下的 200、查询参数 400、非法私钥 503、POST 405 测试。修复后完整网页安装、重启、心跳复测通过。

网关侧额外验证：严格 CA 校验且带 PEM Accept 的 HTTPS 请求返回 200；证书字节与本机公共 CA 一致；查询参数返回 400，POST 被入口白名单拒绝为 404。仅证书首次下载使用无认证指纹引导，注册和心跳仍严格验证证书链和主机名。

## 服务切换与回退

- 确认 Celery 活动任务及活动压测均为零后，修改实际 `.env` 中的发行目录；后端、Celery、网关、性能控制器均已按新路径重启。
- 重启后安装元数据可用、Celery `pong`、控制器在线；CA 修复后另行重启后端并完成上述真实联调。
- 原 `.env` 备份为 `backend/temp/direct-docker-activation-20260918.env.backup`，权限 0600，不纳入 Git。不要公开或整份覆盖该备份；只用于核对本次字段。
- 原发行目录、旧公开镜像和用户节点均保留。若回退，需要同时恢复对应后端/前端/网关代码与原发行目录，再在空闲时重启相关服务；仅改目录不足以让新版命令读取旧 manifest。不得因此删除节点身份卷。

## 未验证边界

- 本轮真实平台安装在本机 Docker（Linux 容器）完成；未声称新版本已在用户 Ubuntu 或 Unraid 机器安装成功，目标网络的 Docker Hub 可达性仍需用户验证。
- 不含远程新一轮压测、长期负载或多节点并发验收；现有 Locust 退出告警仍见技术债记录。
- 一次性令牌会出现在终端历史和 Docker 参数中，有效15分钟；镜像拉取过慢可能需要重新签发命令。长期身份只保存在专用卷。
- 定向敏感内容审计不是完整供应链漏洞审计；公开镜像仅限授权测试。
