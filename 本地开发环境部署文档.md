# AITS系统本地开发环境部署文档

平台的操作手册：https://kxfgae3nvd.feishu.cn/wiki/Dpjyw1ax4iXoC7kVNxVcgd69nrg

## 项目概述

AITS（AI Test System）是一个基于Django + Vue.js的AI测试系统，包含以下主要组件：
- **后端**: Django 5.2.5 + Django REST Framework + ASGI（支持WebSocket）
- **前端**: Vue 3 + Element Plus + Vite
- **数据库**: SQLite（默认，开发环境友好）
- **缓存**: Redis
- **异步任务**: Celery + Celery Beat（支持定时任务调度）
- **AI功能**: LangChain + OpenAI/DeepSeek/Ollama

### 主要功能模块

1. **API测试**: 支持OpenAPI/Swagger文档解析、自动化测试用例生成和执行
2. **Web测试**: 基于Playwright的Web UI自动化测试，支持AI生成测试用例
3. **App测试**: 移动应用测试（开发中）
4. **定时任务**: 统一的定时任务管理系统，支持Web、API、App测试套件的定时执行
5. **项目管理**: 多项目、多环境管理
6. **知识库**: RAG知识库，支持文档上传和向量检索

## 环境要求

### 系统要求
- **操作系统**: Windows 10/11, macOS, Linux
- **Python**: 3.12+
- **JDK**: 1.8+
- **Node.js**: 18+
- **Redis**: 6.0+
- **Git**: 最新版本 -- 并不做要求
- **Conda**: Miniconda 或 Anaconda
- **数据库**: SQLite（Python内置，无需额外安装）
- **定时任务**: Celery + Celery Beat（用于定时任务调度）

### 硬件要求
- **内存**: 最少 4GB，推荐 8GB+
- **存储**: 最少 3GB 可用空间（包含conda环境）
- **网络**: 稳定的互联网连接（用于AI API调用）

## 前置条件安装

### 1. Conda环境安装

#### Windows
```bash
# 下载Miniconda
# 访问 https://docs.conda.io/en/latest/miniconda.html
# 下载并安装 Miniconda3-latest-Windows-x86_64.exe

# 验证安装
conda --version
python --version
```

#### macOS
```bash
# 使用Homebrew安装
brew install --cask miniconda

# 或下载安装包
# 访问 https://docs.conda.io/en/latest/miniconda.html
# 下载 Miniconda3-latest-MacOSX-x86_64.sh
# bash Miniconda3-latest-MacOSX-x86_64.sh

# 验证安装
conda --version
```

#### Linux
```bash
# 下载并安装Miniconda
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 重新加载shell配置
source ~/.bashrc

# 验证安装
conda --version
```

#### 配置conda镜像源（可选，提高下载速度）
```bash
# 配置清华镜像源
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/
conda config --set show_channel_urls yes
```

### 2. Node.js环境安装

#### Windows/macOS
```bash
# 访问 https://nodejs.org/ 下载LTS版本
# 或使用包管理器

# macOS使用Homebrew
brew install node

# 验证安装
node --version
npm --version
```

#### Linux
```bash
# 使用NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 3. Redis安装和配置

#### Windows

windows系统redis软件的版本一般都是小于6.0，不太建议大家直接在windows里面安装redis软件包，我们可以装到docker的环境中

* （你的docker环境可能在云服务器里面，也可以在本地的虚拟机里面）--- 通过远程工具xshell、mobaxterm、finalshell连接服务器
* 如果不想要用云服务器或者虚拟机，你可以在windows里面装docker desktop，里面再部署redis服务

```bash
# 下载Redis for Windows
# 访问 https://github.com/microsoftarchive/redis/releases
# 下载并安装Redis-x64-*.msi

# 或使用WSL2 + Docker
```

#### macOS
```bash
# 使用Homebrew
brew install redis

# 启动Redis服务
brew services start redis
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt install redis-server

# 启动Redis服务
sudo systemctl start redis-server
sudo systemctl enable redis-server
```

> 推荐使用Docker的方式部署redis

```shell
docker run -d --name redis -p 6379:6379 -v redis-data:/data redis:7  redis-server --appendonly yes
```

如果用的是云服务器的同学，记得要开启6379端口



### 后端环境配置

#### 1. 创建Conda虚拟环境

```bash
# 进入项目根目录
cd aits-system

# 创建conda虚拟环境（指定Python版本）
conda create -n aits-backend python=3.12 -y

# 激活conda环境
conda activate aits-backend

# 验证环境
python --version
which python
```

#### 2. 安装Python依赖

```bash
# 进入后端目录
cd backend

# 安装项目依赖 ，在mac/linux环境中不需要安装pywin32==311 ，这个可以去掉
pip install -r requirements.txt

# 安装Playwright浏览器（WebUI测试需要）
playwright install

# 重要提示：requirements.txt 已包含以下关键依赖：
# - celery==5.3.4：Celery异步任务框架
# - django-celery-beat==2.5.0：Celery Beat定时任务调度器（数据库存储配置）
# - croniter==1.4.1：Cron表达式解析库
# - redis==5.0.1：Redis缓存和消息代理
```

#### 3. 环境变量配置

```bash
# 复制环境变量模板
cp env.example .env

# 编辑.env文件，配置必要的环境变量
# Windows用户可以使用记事本或其他编辑器
notepad .env
# macOS/Linux用户可以使用vim或nano
vim .env
# 或
nano .env
```

**重要提示：**
- `.env` 文件包含敏感信息，请确保不要提交到版本控制系统
- 生产环境请使用强密码和安全的密钥
- 开发环境可以使用示例中的默认值

**环境变量配置说明：**

根据 `env.example` 文件，需要配置以下环境变量：

```bash
# Django基础配置
DEBUG=True
DJANGO_SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# 数据库配置（SQLite - 默认）
# 使用SQLite时无需配置数据库连接参数
# DATABASE_URL=sqlite:///db.sqlite3

# PostgreSQL配置（可选，生产环境使用-实际部署到服务器的环境）
# DB_NAME=aits_db
# DB_USER=aits_user
# DB_PASSWORD=aits_password
# DB_HOST=localhost
# DB_PORT=5432

# Redis配置
REDIS_URL=redis://:pythonvip@192.168.5.10:6379/0

# 文件上传配置
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760
```

**开发环境配置建议：**

```bash
# 开发环境推荐配置
DEBUG=True
DJANGO_SECRET_KEY=dev-secret-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,::1

# 数据库配置（SQLite - 开发环境默认）
# 无需额外配置，SQLite数据库文件会自动创建

# 本地Redis配置
REDIS_URL=redis://localhost:6379/0

# 文件上传大小限制（10MB）
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760
```


#### 4. 数据库配置和初始化

**SQLite数据库配置（默认）：**

项目默认使用SQLite数据库，无需额外安装数据库服务器。SQLite数据库文件将自动创建在 `backend/db.sqlite3`。

**数据库初始化：**

```bash
# 创建数据库迁移文件
python manage.py makemigrations ai_core api_testing projects users web_testing scheduled_tasks

# 执行数据库迁移
python manage.py migrate

# 创建超级用户，后续的普通用户走平台的注册接口来实现
python manage.py createsuperuser
```

> 数据库连接工具：DBeaver   https://dbeaver.io/



#### 5. 启动Django开发服务器

```bash
# 启动Django服务器（支持WebSocket）
python run_asgi.py

# 服务器将在 http://localhost:8000 启动
# WebSocket地址: ws://localhost:8000/ws/
```

### 第三步：前端环境配置

#### 1. 安装Node.js依赖

```bash
# 进入前端目录
cd ../frontend

# 安装依赖
npm install
```

#### 2. 启动前端开发服务器

```bash
# 启动开发服务器
npm run dev

# 服务器将在 http://localhost:5173 启动
```

### 第四步：Celery异步任务配置

#### 1. 启动Celery Worker

**Windows系统：**

```bash
# 在backend目录下，激活conda环境后
cd backend
conda activate aits-backend

# Windows启动Celery Worker（使用solo池）
celery -A aits_backend worker --loglevel=info --pool=solo
```

**macOS/Linux系统：**

```bash
# 在backend目录下，激活conda环境后
cd backend
conda activate aits-backend

# macOS/Linux启动Celery Worker
celery -A aits_backend worker --loglevel=info
```

**Windows特殊说明：**
- Windows不支持Celery的默认进程池（prefork），使用solo池
- solo池是单线程池，适合Windows环境，无需额外依赖
- 如果需要更高性能，可以安装eventlet：`pip install eventlet`，然后使用eventlet池

### 第五步：Celery Beat定时任务调度器配置

#### 1. 启动Celery Beat调度器

**Windows系统：**

```bash
# 在backend目录下，激活conda环境后
cd backend
conda activate aits-backend

# Windows启动Celery Beat
celery -A aits_backend beat --loglevel=info
```

**macOS/Linux系统：**

```bash
# 在backend目录下，激活conda环境后
cd backend
conda activate aits-backend

# macOS/Linux启动Celery Beat
celery -A aits_backend beat --loglevel=info
```

**重要说明：**
- Celery Beat用于调度定时任务，必须与Celery Worker配合使用
- Celery Beat读取数据库中的定时任务配置（django_celery_beat应用）
- 定时任务配置存储在数据库中，支持动态添加、修改、删除任务

#### 2. 定时任务功能说明

定时任务中心支持以下功能：

**主要特性：**
- **任务管理**: 创建、编辑、删除、启用/暂停定时任务
- **测试套件支持**: 支持Web、API、App测试套件的定时执行
- **Cron表达式**: 支持标准的Cron表达式配置执行时间
- **执行监控**: 查看任务的执行历史和详细日志
- **手动执行**: 可以手动触发任务立即执行
- **通知配置**: 支持邮件和Webhook通知（可选）

**Cron表达式示例：**

```bash
0 9 * * 1-5        # 工作日9点执行
0 0 * * 0          # 每周日0点执行
0 0 1 * *          # 每月1号0点执行
*/30 * * * *       # 每30分钟执行一次
0 0 1 1 *          # 每年1月1号0点执行
```

## 服务启动顺序

为了确保系统正常运行，建议按以下顺序启动服务：

1. **Redis服务**（如果未自动启动）
   ```bash
   # Windows
   redis-server
   
   # macOS
   brew services start redis
   
   # Linux
   sudo systemctl start redis-server
   ```

2. **Celery Worker**
   ```bash
   cd backend
   conda activate aits-backend
   
   # Windows
   celery -A aits_backend worker --loglevel=info --pool=solo
   
   # macOS/Linux
   celery -A aits_backend worker --loglevel=info
   ```

3. **Celery Beat定时任务调度器**（启用定时任务功能）
   ```bash
   cd backend
   conda activate aits-backend
   
   # Windows/macOS/Linux（命令相同）
   celery -A aits_backend beat --loglevel=info
   
   # 注意：如果只需要异步任务而不需要定时任务，可以跳过此步骤
   ```

4. **Django后端服务**
   ```bash
   cd backend
   conda activate aits-backend
   python run_asgi.py
   ```

5. **前端服务**
   ```bash
   cd frontend
   npm run dev
   ```

## 验证部署

### 1. 后端API验证

访问以下URL验证后端服务：

- **Django Admin**: http://localhost:8000/admin/
- **API文档**: http://localhost:8000/api/v1/
- **健康检查**: http://localhost:8000/api/v1/health/

### 2. 前端界面验证

访问 http://localhost:5173 验证前端界面是否正常加载。

### 3. Redis连接验证

```bash
# 测试Redis连接
redis-cli ping
```

### 4. Celery任务验证

```bash
# 在Django shell中测试Celery
python manage.py shell
>>> from apps.projects.tasks import debug_task
>>> debug_task.delay()
```