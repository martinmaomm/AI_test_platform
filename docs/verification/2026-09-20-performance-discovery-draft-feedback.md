# 性能探索生成草稿反馈修复（2026-09-20）

## 现场诊断

- 项目 4 的探索任务 `1cdae44c-2466-4977-9423-23f1efaf9f02` 已完成，20 条样本可用。登录样本 HTTP 200、`capture_complete=true`，响应正文存在，本次并非登录采集失败。
- 本机后端日志记录了该任务两次 `POST /draft/`，均返回 HTTP 400。当前任务接口来源是 `http://shop.lemonban.com:8107`，唯一压测目标配置为网页地址 `http://mall.lemonban.com:3344/`，来源不匹配。
- 对现有数据只读调用草稿编译器，复现“所选性能目标 origin 与探索任务已确认 origin 不一致”。没有修改目标、历史任务或样本，没有请求商城接口。
- 前端 `usePerformanceDiscovery.action()` 把异常转换为 `null`，只更新页面顶部错误；按钮处理函数收到 `null` 后直接返回，底部用户看不到就地反馈，也不会进入错误弹窗逻辑。

## 修复范围

仅修改前端草稿交互与回归脚本。保留服务端同来源和方法授权检查，不自动改变已批准的压测目标。

来源不匹配时，在生成按钮旁说明任务接口来源与所选目标来源；后端拒绝时显示即时提示及按钮旁的持久错误，保留样本和目标选择供重试。旧项目或旧任务请求的迟到错误不污染当前页面。

## 验证

- 前端针对性单元测试 7/7 通过：来源匹配、默认端口与路径归一化、当前草稿错误透传及 loading 释放、切换项目或任务后的迟到错误丢弃。
- 前端生产构建通过，保留既有大分块提示。ESLint 未配置，本轮未将其计为通过。
- 真实 Vue / Django / Chrome 隔离浏览器验证通过：选中两个样本；选错来源时按钮旁显示两个来源、阻止无效提交；合法来源目标的允许方法在页面加载后发生变化，服务器真实返回 400，页面弹出并保留具体错误、保持两个样本选中、结束 loading；恢复夹具目标允许方法后重试 200，打开含两个步骤的编辑器并成功保存。
- 同时验证默认 900 秒、默认自动允许跨域及关闭选项仍正常。整个隔离流程无页面错误、无模型调用、无压测运行、无目标站请求。
- 浏览器脚本首次在“两步编辑器同时存在两份参数表单”的测试假设处失败；按现有编辑器的步骤切换交互校正定位后完整通过，未改编辑器业务代码。
- `git diff --check` 通过。

浏览器证据：`backend/temp/performance-discovery-browser/draft-error-feedback.png` 和 `imported-draft.png`，均为临时数据库及虚构样本。

## 生效与现场剩余配置

前端代码和构建产物已更新。现场压测目标配置保持原值；生成当前商城接口草稿时，需在「压测目标」配置并选择与 `http://shop.lemonban.com:8107` 同来源的已批准目标。探索入口网页地址和接口压测目标可以不同，不能将网页地址当作接口来源。此轮没有修改现有目标或运行计划。

## 追加：样本请求 URL 展示

- 每条样本的折叠标题直接显示请求 URL，包含实际接口来源、端口、路径及公开查询参数；无需展开 JSON，也不依赖重新探索。长地址自动换行。
- 地址从记录的 origin/path 和服务端公开 query 组合，查询参数保留顺序、重复键、空值与现有脱敏占位符；URL 编码按标准格式生成。没有读取 raw_line、原始 URL 或使用网页入口补全未知接口地址，未授权记录只显示来源与路径。
- 前端状态单测 6/6、生产构建和真实 Vue / Django / Chrome 隔离浏览器验证通过。夹具网页地址与 API 地址刻意不同，两条样本在折叠状态分别显示带不同查询参数的 API URL；原两样本生成、400 提示、重试及保存流程继续通过。
- 已查看局部截图 `backend/temp/performance-discovery-browser/sample-request-urls.png`，两条 URL 完整可见。

## 追加：探索页直接编辑压测目标来源

- 在探索样本下方的目标选择框旁增加“编辑来源”，支持手动修改地址，也可点击“使用本次接口来源”填入当前任务的 API origin。填入不会自动保存。
- 沿用目标管理页的平台管理员权限。显式保存只 PATCH `base_url`，保留名称、已批准的请求方法及目标 ID；弹窗说明该修改会影响引用该目标的计划后续使用的地址。
- 保存成功用服务端返回值更新当前下拉项，保留两个样本和目标选择，立即重新判断来源是否匹配，无需重新探索。失败保留输入并在弹窗内显示具体错误。保存中禁用编辑和用户关闭路径；项目、任务、目标切换及组件卸载会使迟到响应失效。
- 相关前端单测 27/27、最终生产构建、真实 Vue / Django / Chrome 隔离浏览器回归通过。浏览器覆盖取消不提交、非法 URL 本地阻止、带业务路径地址由服务端返回 400、挂起保存期间禁止编辑和关闭、模拟 503 后保留输入并重试、真实 PATCH 200 后 GET 回读，以及保持目标和样本选择继续生成两步草稿并保存计划。原草稿方法授权 400 提示和重试仍通过。
- 浏览器脚本首次在保存后截图时因 `.detail .actions` 匹配两处操作区而失败；收窄为包含目标选择框的操作区后完整通过。无页面错误、模型调用、压测运行或目标站请求；未修改现场目标配置。
- 已查看截图 `backend/temp/performance-discovery-browser/target-source-edit-error.png` 和 `target-source-edited.png`，编辑表单、持久错误、更新后的地址及草稿按钮均清楚可见。
