# 优化与技术债跟踪

## 性能测试分批交付边界（2026-09-16）

- 第二批接入独立 Master/Worker、冻结快照、全局单运行租约、每轮 mTLS 准入、停止及基础统计。默认未启用执行；必须完成独立控制器/TLS 入口配置，节点在线不等于 Worker 就绪。
- 当前一轮一个节点、固定结构化请求模板；多节点分摊、API 场景转换、高级报告与长期日志保留策略待后续批次。不接受用户 Python，非 root 不等于任意脚本沙箱。
- 平台 HTTPS 入口、远程 Linux 安装、amd64、公网和混合节点均待专项部署验收；不放开原生 Locust RPC、不关闭控制通道证书校验。
- 节点注册响应丢失或身份落盘失败时，不自动重复创建身份；管理员重置凭证后重新登记。状态目录必须专用，客户端不修改已有宽权限目录的权限。后续运行器需单独验证不可信脚本隔离，不把非 root 等同于脚本沙箱。
- 验证记录见 [第一批性能验收](verification/2026-09-16-performance-node-access.md)、[第二批执行验收](verification/2026-09-16-performance-execution.md)。

## APP 自动化退役后的存储清理（暂不执行）

- 2026-09-16：旧 APP 页面、导航、MidScene HTTP / WebSocket 入口、Celery 任务和智能体已移除，项目与环境不再接受 APP 类型。共享 UI/API 执行、取消、流式输出仍保留。
- 本次只清理代码与可选类型；保留 `MidSceneScript` 表映射和历史迁移，不执行删除业务记录或删表。当前库核对为 0 个 APP 项目、0 个 APP 环境、0 条 MidScene 脚本；这个结果不代表其他部署也没有数据。
- 后续如需彻底移除存储模型，应独立盘点目标库并确认删表范围；不能直接改写初始迁移。本次 `projects.0006_retire_app_choices` 只更新 choices，MariaDB `sqlmigrate` 为 no-op。

## 旧 UI 自动化测试夹具与当前权限契约不一致（待维护）

- 2026-09-16 APP 退役扩大回归：提交前基线 `fbc2420` 的隔离快照运行 `web_testing` 541 项，当前代码运行 545 项；两边均有 51 failures、3 errors，逐项对比失败测试 ID 集合完全一致，未新增失败。
- 多处旧夹具只有创建者、没有项目成员，接口返回 404 后，报告/修复断言继续读取 `data` 或 `repair_availability` 而报错。维护时须区分夹具过时与实际缺陷，不应为让旧测试通过而放宽权限或公开管理接口。
- 本次新增退役路由、任务注册、WebSocket 与取消回归通过；全套旧测试仍不能标记为全绿。后续单独补齐正向夹具与当前报告契约，保留非成员和撤权测试。

## MCP 单配置与并发边界

- 2026-09-15 调整：按用户确认收敛为一份全局 Playwright MCP 配置。只有未配置时可添加，已有配置只允许编辑、启停、检测，不支持新增其他版本或删除；数据库常量槽唯一约束防止并发重复创建。升级通过编辑完成，工具检测状态不等于运行时健康检查。
- 当前部署为单个 Celery `--pool=solo` Worker，探索任务排队；工具调用本身也按顺序执行。每项探索创建独立 stdio MCP 会话并按任务保存输出。此事实不保证增加 Worker 后仍然全局串行，也不表示所有第三方远程 MCP 都具备会话隔离。
- 扩展并发前再明确全局并发上限、测试账号及数据隔离。浏览器会话分离不能阻止两个任务修改同一条业务数据，或被测系统限制同一账号同时登录。单份配置不限制用户数量，也不代表所有第三方 MCP 的会话隔离已经验证；暂不引入调度重构。

## 首页 AI 来源统计与历史数据边界

- 2026-09-15：初始来源固定到两类用例的 `creation_source`。首页不再从当前脚本来源/工作区状态临时推导占比，后续编辑、AI 修复和删除工作区不改初始来源。不能把场景类型、模型选择或一次调试通过直接当作 AI 来源。
- 历史边界：旧记录没有完整的初次保存来源事件，只按保留的生成关联与时间证据回填；缺少证据的为 `unknown`，计入总数但不推断为 AI，也不假定为手工。不可为了提高占比强行猜测回填。
- UI 无用例关联的草稿调试按执行编号展示，不把同名草稿错误合并。定义及验收见 [固定初始来源](verification/2026-09-15-case-creation-source.md)。

## 旧 API 自动化测试夹具缺少项目成员（待维护）

- 2026-09-15 扩展回归发现：`api_testing.test_workspace`、`api_testing.test_endpoint_workspace` 部分测试仍仅设置项目创建者，没有按新的成员权限模型分配成员，因而在 403 分支失败；其中一项因读取被拒绝响应的 `data` 而报错。
- 同条件在修改前的 `e68b168` 独立临时快照与当前代码各运行 147 项邻接测试，均为 45 失败、1 错误，失败测试 ID 和错误相同，没有新增失败。不能将这批旧测试描述为已通过。
- 后续仅更新需要正常操作权限的测试夹具，保留非成员/撤销权限的负向测试，不放宽实际项目权限。本次来源测试使用明确的项目成员配置，在隔离环境独立验收。

## 正式应用前：报告访问、多用户权限与隐私

- 记录日期：2026-09-15；状态：报告与隐私策略暂缓，P-02 的简单用户管理已按方案 A 实施；正式应用前仍需重新评审与验收。
- 当前决定：调试阶段允许独立报告匿名只读访问，接受报告中测试数据、日志、截图及测试凭据可见。此决定不代表项目配置、脚本修改、执行、删除或 AI 修复也可以公开；这些管理权限继续保留。
- 报告实现与验证见 [报告公开访问验收](verification/2026-09-15-public-reports.md)。下列条目分别标明已实施与后续待办，不表示所有列出的风险均已复现。

### P-01 报告访问与分享策略（待设计）

- 现状：知道或猜到报告地址即可读取内容，无登录、项目成员、分享密码或到期时间限制；转发链接也能访问。
- 上线前确定：内部报告是否恢复登录和项目权限；外部分享是否采用不可预测、可撤销、可过期的授权链接。截图、关联子报告和下载资源必须遵守同一授权规则，不能只保护页面入口。
- 验收：覆盖匿名、非成员、成员被移除、链接过期/撤销、猜测 ID 和直接访问附件；分享访问不得获得管理权限。撤销无法收回接收者已下载或另存的内容，需明确说明。

### P-02 多用户管理与权限边界（简单版已实施）

- 按用户确认的方案 A，仅保留管理员、普通用户两个可选角色，初始化超级管理员受保护且独占管理员授予权；管理员管理全部项目、普通用户操作已分配项目。未建设项目管理员或报告查看者等额外角色。
- 已提供账号创建/编辑/启停/密码重置和多人多项目分配；后端统一项目权限、共享可用 AI 配置并隔离配置密钥。注册或个人资料不能提权，停用后的旧 token 不能用于新请求/新建 WebSocket；项目移除和角色降级按最新关系检查。
- 使用方法见 [用户管理与项目分配](user-management.md)，设计见 [方案 A](plans/2026-09-15-simple-user-management.md)。公开报告策略不变；正式应用前仍需决定更强会话撤销、正在运行任务的停用策略和操作审计。

### P-03 敏感信息与凭据处理（待评估、待实施）

- 检查报告、请求响应、运行日志、生成/修复对话、浏览器轨迹、截图、导出文件及邮件摘要中的密码、Token、Cookie、API Key 和个人信息。
- 上线前确定凭据引用、存储及展示规则；区分内部调试详情与可分享报告。截图需要单独处理，文本替换不能代替图像隐私检查。
- 评估传给第三方模型/MCP 的数据范围，避免无关信息随上下文发送；用明确标记的测试数据验收各输出渠道，不以关键词过滤阻断正常探索和生成流程。

### P-04 历史数据与保留期限（待评估、待实施）

- 新规则不能自动消除已有明文数据。盘点数据库、日志、截图、轨迹、导出及备份，确定保留期限、访问方式和清理范围；需要时轮换曾公开的凭据。
- 历史清理或凭据轮换须另行确认具体对象和影响，不因本条记录自动删除数据。验收同时检查旧报告、旧附件及备份，不只检查新生成记录。

### P-05 报告链接与上线配置（已明确暂缓）

- 报告链接地址问题按用户要求暂不修改。正式部署时核对 `FRONTEND_BASE_URL`、域名、HTTPS、反向代理和开发模式配置，确保邮件链接对实际收件人可达。
- 验收：从不同设备打开邮件中的真实报告链接；按届时确定的访问策略检查报告和截图，不以本机打开成功代替收件人访问验证。

除 P-02 的方案 A 用户管理外，上述事项仍仅记录；不重写当前生成、调试、执行流程，不重新启用隐私拦截，不清理历史数据或发送测试邮件。

## API 第二批真实联调问题

- 发现日期：2026-09-14；原始复现见 [第二批真实联调报告](verification/2026-09-14-api-regression-batch2.md)，后续结果见 [修复与定向复测](verification/2026-09-14-api-regression-fixes.md)。
- D-01 多场景目标混入（P1，已修复）：子场景不再继承根对话，规划明确共享约束与单场景目标；修复反馈指出冻结认证契约冲突，严格断言保护保留。真实根 28 的两个子场景独立运行通过，没有再混入其他场景。
- D-02 计划报告排序（P1，已修复）：统一快照创建明细，按冻结顺序返回报告。真实新执行 86 的报告/快照/创建/实际顺序均为 10→11；旧报告 79、80 的显示也已修复，未改写执行数据。
- D-03 同值更新导致业务验收假通过（P1，已定向修复）：生成契约明确系统变量单次执行固定；需要真实变化时使用不同值、保存实际旧值并验证新值和前后差异，幂等场景除外。子 29 原断言保留，审核后调试 87 和独立执行 88–90 均通过，已保存用例 14；逐次确认实际改名与清理，既有数据和账号权限未变。[修复与验收边界](verification/2026-09-14-api-state-change-fix.md)。不使用固定关键词、不取消断言保护，不自动补改任意旧用例。
- 当前不能放行全部 API 模块：真实邮件及第三批剩余项仍需处理。

## API 自动来源确认与 HTTP 重定向

- 发现日期：2026-09-09。
- 范围：API 网页探索自动模式的跨主机来源确认；不是 requests 执行层或 UI 脚本生成层。
- 根因：当前 Chromium Playwright 路由仅将首跳交给 `context.route`，已放行请求的后续 HTTP 重定向由浏览器继续，不能靠首跳授权宣称拦截了全部网络请求。307/308 可能转发原方法与正文。
- 本次处理：直接 fetch/XHR 请求支持发送前确认，重定向后续仅保留受限元信息，不作为完整可执行证据；页面和使用文档明确标注仅限可信测试站点，不支持把此机制当作重定向或 SSO 的安全隔离层。
- 已知风险：重定向请求可能在用户审批前已经发出，元信息记录不能撤销网站写入。不能通过重复登录、代理重放或补抓正文掩盖此限制。
- 后续根因方案：单独评估 Chromium CDP 响应阶段 Fetch 拦截与 Playwright 路由的交互，在重定向发生前确认目标；须测试 302/303/307/308、相对 Location、多跳、Cookie、方法与正文保留、取消及无重放，再改变支持范围。本次未引入该底层架构改动。
- 2026-09-12 回归：已补齐直接文档导航和新窗口首请求的来源检查。尝试响应代取后发现 Chromium 将内网页面当作合成响应，导致原本可用的回环 API 被网络地址空间策略拒绝；该实现已撤回，不授予额外浏览器权限、不禁用网络安全功能。11 项采集测试（含真实 Chrome）通过；重定向仍保持上述边界。

## Element Plus 数字输入框动态禁用状态

- 发现日期：2026-09-09。
- 范围：API 网页探索页的探索总时限输入框。
- 触发：输入框初次挂载时页面仍在加载，`disabled=true`；随后解锁。
- 本地证据：当前安装版本 `element-plus/es/components/input-number/src/input-number2.mjs` 只在 `onMounted` 写入内部输入框的 `aria-disabled`，`onUpdated` 只更新 `aria-valuenow`。实际可编辑状态恢复，但可访问性状态仍为禁用。
- 当前处理：按禁用状态为该数字输入框设置组件 key，状态变化时重建控件，使原生状态与 ARIA 状态同步；数值由父级 `v-model` 保留。不改依赖包文件，不升级全项目依赖，不强制绕过浏览器的可用性检查。
- 边界：切换禁用状态会重建该控件并丢失其焦点；加载或提交期间本就不允许输入，已输入数值仍保留。
- 后续：统一升级组件库时，验证动态 `aria-disabled` 已修复后移除此局部处理，保留浏览器回归测试。
