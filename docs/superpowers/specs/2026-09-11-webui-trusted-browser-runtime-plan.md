# UI 脚本生成：可信浏览器运行层整理计划

日期：2026-09-11。用户已确认方案 B；按批次交付，不同时重写全部生成流程。第一批已完成本地验收，第二批最小实现及第三批两类页面的真实重复性验收现已完成。具体范围和不能覆盖的情形见验收记录，不等同于任意网站成功率保证。

后续授权与进展（2026-09-11）：用户允许使用平台现有“思辰科技 · gpt-5.6-luna”进行真实测试及修改测试环境数据；因此下文第一批的“不重启、不付费、不写测试网站”仅描述第一批边界，不限制这次已授权验收。第二批已加入真实唯一数据、当前页面 selector 核验、多匹配操作阻止、完整操作历史和无损源码提取；第三批在商城资源流程与英文原生表单各完成两次独立运行。详细失败与复测记录见 [验收记录](../../verification/2026-09-11-ui-generation-live-acceptance.md)。以实际生成、独立脚本运行和证据为准，不以离线测试替代真实通过。

## 目标与问题证据

保留单智能体、单浏览器上下文、串行操作及独立 Python 脚本。让模型负责理解目标和业务判断，让平台负责可验证的页面观察、确定性操作、数据和事实记录。

本地审查和 Chromium 最小复现确认：

- 当前 MCP 可见文字读取只检查直接父节点，隐藏祖先里的弹窗文字仍会返回；HTML 读取返回完整 DOM，不能证明可见。
- 页面投影按前 8000 字符裁剪，可能留下导航、丢掉后面的当前表单。本次实际轨迹重放出现这一情况。
- 页面防重复指纹取整个工具返回值，诊断时间戳变化会被误判为页面变化。
- 当前探索工具缺少确定性唯一值生成接口；写在提示词中的 `time.time_ns()` 不等于真实执行。
- 轨迹与现有草稿能持久化，但模型未编写业务草稿时，保存轨迹不等于生成了对应代码。

前两项和指纹联动是第一批范围；动态数据和代码衔接留到第二批，不能提前宣称解决。

## 保留与不做

保留任务队列、取消与恢复、版本校验、草稿持久化、调试截图和用户确认保存。继续区分生成结束、草稿完整、当前版本实际调试通过。

不新增多智能体业务编排，不恢复强制 ScenarioPlan JSON 定稿，不使用固定网站字段或中英文业务按钮词表，不调整模型、依赖版本或调用上限，不修改 API 网页探索及数据库模型。此次不重启服务、不运行付费模型或被测网站写操作，不迁移旧生成记录。

## 统一职责

1. 浏览器端观察：当前 URL、页面标题、可见控件的语义和可用状态、所属区域、可见文字、观察范围与不完整性。
2. 平台投影：结构化压缩而非机械截断；模型实际收到与浏览器观察一致的关键信息。
3. 状态判断：页面语义状态与诊断信息分开；不同读取格式和不同观察范围不构成业务进展。
4. 工具结果：操作完成不等于业务断言成功；观察失败不重放已经执行的操作。
5. 后续数据与代码：本轮实际值关联代码中的生成表达式；操作证据与草稿进度分别记录，不把尚未确认的断言伪装为通过。

## 第一批：浏览器观察与状态基础

### 接口与替换方式

通过项目现有的 `MCP_PAGE_DIAGNOSTICS=1` 受控 MCP 启动入口，为 UI 生成替换可见 HTML/文字工具的读取实现。不修改 npm 缓存中的包，不另起浏览器；关闭该 opt-in 的其他流程维持原有工具行为。

两种读取工具共享一份浏览器采集逻辑。页面整体观察优先当前可见对话框、表单、主体控件，再提供导航及文字。支持根据 selector 局部补读；目标隐藏、不存在或有多个匹配时明确返回不能确定，不能拿第一个结果当作唯一目标。

采用有界等待采样，不能依赖永远不会结束的 network-idle。稳定只表示采样窗口内的观察稳定，不证明服务端写入完成。无法等到稳定时返回明确状态，不自动重复点击。

### 内部数据约定

现有 `PLATFORM_BROWSER_DIAGNOSTICS_V1` 帧追加 `observation`，由平台产生，不要求大模型填写：

```text
version: 1
page_url / page_title / scope
fingerprint: 全局可见语义状态的 SHA-256
settled / truncated / notes
elements: [{tag, role, name, id, type, placeholder, visible, enabled, container}]
text: [可见文字]
```

观察体有大小与数量限制，字段按结构裁剪并标明缺失；不得把截断后的缺失视为页面不存在该元素。返回值不输出输入框当前密码。URL/标题、真实控件状态和文字变化可以改变指纹；时间戳、截图文件名、工具调用 ID、读取格式、局部观察范围不能改变指纹。不同作用域读取使用同一个全页状态口径。

`truncated` 指本次观察范围的完整性。全页较大而局部表单完整时，局部读取可以返回 `truncated=false`；全局指纹采样受限以 `global_state_partial` 单独注明，不能让模型因为全页过大而无休止重读已经完整的局部区域。

Python 在去掉传输帧之前先提取并校验观察信息，再生成模型可读的紧凑摘要。错误诊断里的可见/可用状态不得与传输帧一起丢弃。自动观察和显式读取共用同一处理路径，不重复计算浏览器调用或改变串行顺序。

### 第一批验收清单

- [x] 祖先隐藏的弹窗、标题、字段不会被报告为当前可见。
- [x] 长导航后面的当前表单仍出现在压缩后的模型输入中。
- [x] 标签、placeholder、禁用状态及所属容器来自真实 DOM。
- [x] 隐藏、多匹配或不存在的局部目标明确失败，不猜测唯一目标。
- [x] 同一页改变时间戳、截图名、HTML/文字读取方式或局部 scope，不推进页面状态。
- [x] 真实弹窗打开/关闭、页面文字或输入状态变化能改变页面状态。
- [x] 延迟页面变化被有界观察捕获，或明确标记观察不稳定；不重放写操作。
- [x] 截断、读取失败、结构不合法时明确不确定；上下文投影不破坏工具问答配对。
- [x] 真实 Node MCP 读取 → 序列化 → Python 提取/投影 → 防重复守卫的跨层测试通过。
- [x] 原有失败截图、调用计数、串行与取消/草稿测试仍通过。

以上是已覆盖的固定回归场景，不代表所有网站和所有 CSS 布局都经过验证。

## 第二批：操作、动态数据与草稿衔接

第一批统一的是观察状态，不声称已经统一所有交互目标的身份。继续整理操作入口：把定位目标与当前观察关联，明确过期、隐藏、多匹配以及执行结果未知的处理；同一元素改写 CSS 不应被当成全新的业务尝试。该部分需要在第一批基础上确定最小可执行接口，不引入网站按钮词表。

为探索提供平台生成的动态值及本轮引用，使创建、修改、查询共享同一实际数据，生成 Python 使用对应表达式而非固定样本字面量。不能通过固定业务字段名判断哪些数据要唯一。

明确工具执行记录、业务验证证据、已保存代码三个层次。改善增量代码生成和中断后的交付，但不自动把一次点击转换成成功断言；保留用户修改、待补充断言及当前版本验证失效规则。

验收：同轮一致、不同轮重新生成；中途停止仍有清楚的步骤/证据/草稿关系；不重放已知已执行的写入；不通过删除断言制造通过。

实际交付采用最小接口：直接提供当前浏览器核验过的 selector、只读/原生选项等事实；不是一套跨页面长期有效的元素 ID，也不声称能识别所有等价 CSS 或动态页面变化。动态值通过本地工具确定性生成和持久化；有界成功操作历史保留定位与确认顺序。模型仍负责业务判断，平台不从按钮词表推断断言。候选静态拒绝不会覆盖旧草稿，也不再显示为新整理成功。

## 第三批：重复性与成本验收

先建立固定场景及允许写入范围，再在至少两个不同结构的网站上重复执行。探索与脚本运行使用独立新会话，记录生成成功率、业务覆盖、重复操作数、浏览器与模型调用数、token 和实际执行结果。

真实站点、多轮模型调用及产生/清理测试数据需要单独明确测试范围；本轮离线测试不代表这部分已通过。不以一轮成功宣称稳定，不承诺所有模型和网站都能无人工修改生成。

已按后续授权实测：商城探索 41 次浏览器/37 次模型调用，初稿有错误，增加操作历史后基于轨迹再整理 1 次；期间定位并修复平台源码提取错误。恢复原始 AI 输出后两次平台调试均通过，每次 9 条断言。英文 preferences 场景 7 次浏览器/13 次模型调用，生成后两次独立运行通过，每次 2 条断言。未取得提供商的实际 token/账单统计，不能用字符压缩比例宣称费用降低或给出通用生成成功率。

页面指纹只表示观察到的语义内容变化，不证明某一步业务操作成功；页面自己的时钟、轮询内容等真实变化可能影响指纹，仍需保留总调用和总时限保护。第一批不能宣称完全杜绝模型重复操作。

## 验证与回退

Node 真实浏览器测试仅使用内存页面或回环地址，不访问 NAS、模型服务。Python 使用 `backend/scripts/test_webui_generation_offline.py`，隔离 SQLite、内存缓存并阻断外网；不使用指向现有 NAS 配置的普通数据库测试。

不作数据库迁移；第一批提交可以整体 revert，重启相应服务后恢复旧运行逻辑。生产使用前先用第一批固定用例验收。服务由用户重启，代码变更不影响已经加载旧实现的进行中任务。

## 交付记录

第一批完成：

- 新建 `backend/scripts/playwright_mcp_observation.mjs`，让观察采集与原有错误诊断分开；保留原诊断辅助函数及失败截图。
- Python 消费端、模型投影、防重复守卫与持久化轨迹共用经过校验的观察指纹。
- 补齐真实跨层 fixture，包括局部范围、长导航、父级禁用、隐藏内容、精确 selector 字面量、多匹配/缺失目标与失败截图。
- 模型仍只在实际调试通过后标记当前版本通过；未调整生成后的断言、业务步骤或运行层。

2026-09-11 主 Agent 验收（macOS，本机已安装的 Chrome 与固定 MCP 包）：

| 验证 | 结果 |
| --- | --- |
| `web_testing`、AI runtime/budget/guard/output 与 API browser discovery 相关离线回归 | 561 项通过 |
| Node diagnostics、observation、output bootstrap 测试 | 15 项通过，0 跳过 |
| `ai_core.tests.test_browser_observation_bridge` 真实浏览器跨层测试 | 7 项通过，0 跳过 |
| JavaScript 语法检查、Python 编译、`git diff --check` | 通过 |

真实浏览器测试使用 `PLAYWRIGHT_MCP_TEST_PACKAGE_ROOT` 指向本机已安装的 MCP 包，`PLAYWRIGHT_CHROME_EXECUTABLE_PATH` 指向现有 Chrome。bootstrap 测试还要求 npm exec 的 PATH，可通过已有缓存的 `npx --offline --yes --package @executeautomation/playwright-mcp-server@1.0.12 -- node --test ...` 执行。测试不自动下载浏览器；跨层测试未配置路径时会跳过，因此 CI 必须核对真实浏览器测试没有跳过。

边界：iframe 与 shadow DOM 本批不递归探索，检测到未遍历内容时明确标记观察不完整。采样为三次、间隔 80ms，单次 locator 读取配置 1200ms 超时；稳定状态只描述该采样窗口，不能替代业务等待或业务断言。不输出当前密码值。

本次没有运行真实模型/被测网站的完整生成，未消耗项目配置的模型 API 额度，不宣称真实生成成功率或 token 降幅已验证。服务未重启，数据库未变更，资源压缩包未纳入提交。用户重启后端与 Celery 后，新生成任务使用新实现；无需数据库迁移或重装浏览器。

以上“未运行真实模型、服务未重启”的描述是第一批交付快照。后续第二、三批的实现、真实模型失败历史、最终通过证据和已重启服务情况，以文首链接的验收记录为准；没有数据库迁移或旧数据兼容改动。
